import { TenantConfig } from '../models/tenant.model';

export const tenantConfig: TenantConfig = {
  mode: 'multi', // Toggle to 'single' for custom domain / dedicated builds
  defaultCompanySlug: 'aliensverse'
};
